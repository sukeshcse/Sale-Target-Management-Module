# Copy to .env and adjust as needed.
# If using the provided docker-compose.yml (postgres service): the URL below matches it as-is.
# If using a local Postgres install instead, replace user/password/db to match your setup.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/sales_target_db?schema=public"

PORT=3001
CORS_ORIGIN="http://localhost:3000"
